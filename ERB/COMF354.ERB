@COM354
;邪魔しない
;%NTR_NAME(0)%と住人１人同席、かつ主人が空気の時の時間進行用コマンド
;会話
IF FLAG:25 == 0
	IF TALENT:NTR
		PRINTFORML %CALLNAME:TARGET% is ignoring %CALLNAME:MASTER% while enjoying %HIS_HER(TARGET)% conversation with %NTR_NAME(0)%.
	ELSE
		PRINTFORML %CALLNAME:TARGET% tried to speak to %CALLNAME:TARGET%, but being overwhelmed by %NTR_NAME(0)%, %CALLNAME:TARGET% couldn't even mutter a word.
	ENDIF
;キス
ELSEIF FLAG:25 == 1
	IF TALENT:NTR
		PRINTFORML %CALLNAME:MASTER% watches in a blank surprise as %NTR_NAME(0)% and %CALLNAME:TARGET% are passionately kissing each other,
		PRINTFORML While enjoying %CALLNAME:MASTER%'s astonished reaction.
	ELSE
		IF TALENT:TARGET:浮気公認
			PRINTFORML %CALLNAME:MASTER% watches in a blank surprise as %NTR_NAME(0)% and %CALLNAME:TARGET% are passionately kissing each other in front of %HIM_HER(MASTER)%.
		ELSE
			PRINTFORML %CALLNAME:MASTER% tried to stop %NTR_NAME(0)% from kissing %CALLNAME:TARGET%, 
			PRINTFORML But being overwhelmed by %NTR_NAME(0)%, %CALLNAME:MASTER% couldn't do anything.
		ENDIF
	ENDIF
	SOURCE:MASTER:欲情 += 20 + 20 * ABL:MASTER:欲望 + 10 * TALENT:TARGET:浮気公認
;愛撫 or 胸愛撫
ELSEIF FLAG:25 == 2 || FLAG:25 == 3
	IF TALENT:NTR
		PRINTFORML %CALLNAME:MASTER% watches in a blank surprise as %CALLNAME:TARGET% receives caress and petting from %NTR_NAME(0)%, 
		PRINTFORML While they proceed totally ignoring %HIM_HER(MASTER)%.
	ELSE
		IF TALENT:TARGET:浮気公認
			PRINTFORML %CALLNAME:MASTER% watches in a blank surprise as %CALLNAME:TARGET% being caressed by %NTR_NAME(0)% in front of %HIM_HER(MASTER)%.
		ELSE
			PRINTFORML %CALLNAME:MASTER% tried to stop %NTR_NAME(0)% from caressing %CALLNAME:TARGET%, 
			PRINTFORML But being overwhelmed by %NTR_NAME(0)%, %CALLNAME:MASTER% couldn't do anything.
		ENDIF
	ENDIF
	SOURCE:MASTER:欲情 += 30 + 30 * ABL:MASTER:欲望 + 20 * TALENT:TARGET:浮気公認
;フェラ or 69
ELSEIF FLAG:25 == 4 || FLAG:25 == 5 || FLAG:25 == 6
	IF TALENT:NTR
		PRINTFORML %CALLNAME:MASTER% watches in a blank surprise as %CALLNAME:TARGET% services %NTR_NAME(0)%, 
		PRINTFORML While they proceed totally ignoring %HIM_HER(MASTER)%.
	ELSE
		IF TALENT:TARGET:浮気公認
			PRINTFORML %CALLNAME:MASTER% watches in a blank surprise as %CALLNAME:TARGET% services %NTR_NAME(0)% in front of %HIM_HER(MASTER)%.
		ELSE
			PRINTFORML %CALLNAME:MASTER% tried to stop %NTR_NAME(0)% from pushing his %PENIS_APPEAR_SIMPLE()% against %CALLNAME:TARGET%'s cheek,
			PRINTFORML But being overwhelmed by %NTR_NAME(0)%, %CALLNAME:MASTER% couldn't do anything.
		ENDIF
	ENDIF
	SOURCE:MASTER:欲情 += 50 + 50 * ABL:MASTER:欲望 + 30 * TALENT:TARGET:浮気公認
;セーックス！おああなる
ELSE
	IF TALENT:NTR
		PRINTFORML %CALLNAME:MASTER% watches in a blank surprise as those two proceed having sex right in front of %HIM_HER(MASTER)%, as if showing it off just to spite %CALLNAME:MASTER%.
	ELSE
		IF TALENT:TARGET:浮気公認
			PRINTFORML %CALLNAME:MASTER% couldn't say anything about love affair between %CALLNAME:TARGET% and %NTR_NAME(0)%.
		ELSE
			PRINTFORML Being extremely shocked, %CALLNAME:MASTER% couldn't say anything about love affair between %CALLNAME:TARGET% and %NTR_NAME(0)%.
		ENDIF
	ENDIF
	SIF !RAND(9999)
		PRINTFORML Woooohooooooo, you just got fucking CUCKED boi! Holy shit it's time to go full reeee am I right?\nAlso this message is super fucking rare so you could win a lottery instead you know?
	SOURCE:MASTER:欲情 += 100 + 100 * ABL:MASTER:欲望 + 50 * TALENT:TARGET:浮気公認
ENDIF

LOSEBASE:気力 += 100
DOWNBASE:MASTER:気力 += 100

TIME += 5
RETURN 1

